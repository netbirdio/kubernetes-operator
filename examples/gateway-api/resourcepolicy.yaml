apiVersion: netbird.io/v1alpha1
kind: ResourcePolicy
metadata:
  name: nginx
  namespace: default
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: nginx
  groups:
    - k8s-demo
