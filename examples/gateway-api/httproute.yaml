apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx
  namespace: default
spec:
  hostnames:
  - nginx-test.sv.stage.npeer.io
  parentRefs:
  - name: netbird
    namespace: netbird
  rules:
    - backendRefs:
        - name: nginx
          port: 80
